## Harness: claude-agent-sdk
### OC-SKILLS-1 — confirmed

**Claim (safe to cite):** OpenClaw's docs explicitly claim AgentSkills (agentskills.io) spec compliance, in a Note inside the SKILL.md format section of docs/tools/skills.md.

**Evidence:** openclaw@203a896:docs/tools/skills.md#L248-L253 — "OpenClaw follows the [AgentSkills](https://agentskills.io) spec. The frontmatter parser supports **single-line keys only** — `metadata` must be a single-line JSON object." Also openclaw@203a896:src/skills/loading/session.ts#L288 "See: https://agentskills.io/integrate-skills" and openclaw@203a896:docs/cli/migrate.md#L166 "Personal AgentSkills under `$HOME/.agents/skills`".

**Note:** Found via `grep -rni "agentskills" docs/ skills/ src/`. Caveat for the gap analysis: the same Note's 'single-line keys only' constraint is stale relative to the shipped parser (see OC-SKILLS-2).

### OC-SKILLS-2 — corrected

**Claim (safe to cite):** Top-level SKILL.md frontmatter fields actually consumed are: name, description (required; local-loader), user-invocable, disable-model-invocation (invocation policy), command-dispatch, command-tool, command-arg-mode (each also accepted with underscores: command_dispatch/command_tool/command_arg_mode), homepage plus aliases website and url, emoji, and metadata (a string parsed as JSON5, reading the vendor-namespaced "openclaw" manifest key plus legacy key names). The parser is NOT single-line-only: parseFrontmatterBlock runs a full YAML.parse (core schema) and flattens structured values (objects/arrays) to single-line JSON strings, with a line-based fallback that also supports indented multi-line values; every value is coerced to a string. Unknown top-level fields are silently ignored — no error and no warning; they stay in the parsed Record but nothing validates or reports them.

**Evidence:** Fields: openclaw@203a896:src/skills/loading/local-loader.ts#L62-L63 ("const name = frontmatter.name?.trim() || fallbackName; const description = frontmatter.description?.trim();"); src/skills/loading/frontmatter.ts#L210-L220 (user-invocable, disable-model-invocation); src/skills/discovery/command-specs.ts#L137-L158 ("entry.frontmatter?.[\"command-dispatch\"] ?? entry.frontmatter?.[\"command_dispatch\"]", command-tool/command_tool, command-arg-mode/command_arg_mode); src/shared/entry-metadata.ts#L14-L20 (homepage "?? params.frontmatter?.website ?? params.frontmatter?.url", emoji); src/shared/frontmatter.ts#L30-L58 (metadata string via getFrontmatterString(.., "metadata") then JSON5.parse, MANIFEST_KEY + LEGACY_MANIFEST_KEYS). Parser: packages/markdown-core/src/frontmatter.ts#L56-L58 (YAML.parse), #L43-L48 (objects -> JSON.stringify), #L80-L101 (extractMultiLineValue), #L200-L231 (merge). Unknown fields — absence of validation: `rg -ni "unknown.*frontmatter|frontmatter.*unknown|unrecognized" src/skills packages/markdown-core/src` returned only two irrelevant hits (a `value: unknown` type annotation and a type cast), no warn/error path; local-loader.ts#L53-L65 checks only name/description.

**Note:** Desk research was incomplete and partly stale: it missed name/description/emoji, the website/url homepage aliases, command-arg-mode, and the underscore variants. The 'single-line YAML parser' constraint no longer holds — the parser at this SHA does full YAML with multi-line support (structured values flattened to JSON strings). The docs Note (skills.md:249-252) still claims single-line-only and is contradicted by the docs' own multi-line metadata example at skills.md:293-305 and by 48/52 bundled skills using multi-line metadata blocks. src/config/types.skills.ts exists but only holds skills config (discovery/limits/overrides) for openclaw.json, not the frontmatter parser.

### OC-SKILLS-3 — confirmed

**Claim (safe to cite):** The in-tree skills/ directory contains 52 skills (52 SKILL.md files, one per directory). Top-level frontmatter key usage across all 52: name 52, description 52, metadata 48, homepage 33, user-invocable 1. Non-spec top-level fields are common in practice: `homepage` appears in 33/52 skills and `user-invocable` in 1/52; the 48 `metadata` blocks are multi-line flow-JSON namespaced under "openclaw". Representative examples: skills/peekaboo/SKILL.md (top-level homepage + metadata.openclaw with os/install), skills/gh-issues/SKILL.md (user-invocable: true + metadata.openclaw), skills/github/SKILL.md (multi-line metadata.openclaw block only).

**Evidence:** Count: `find skills -name SKILL.md -maxdepth 2 | wc -l` = 52. Key tally via awk over frontmatter blocks of skills/*/SKILL.md: 52 name, 52 description, 48 metadata, 33 homepage, 1 user-invocable. Examples: openclaw@203a896:skills/peekaboo/SKILL.md#L4 ("homepage: https://peekaboo.boo"); openclaw@203a896:skills/gh-issues/SKILL.md#L4 ("user-invocable: true"); openclaw@203a896:skills/github/SKILL.md#L4-L13 (multi-line `metadata:` with "openclaw": {"emoji": "🐙", "requires": {"bins": ["gh"]}, ...}).

**Note:** The bundled skills' multi-line metadata blocks directly contradict the docs' 'metadata must be a single-line JSON object' note — useful for the gap analysis. 'Non-spec' here means fields beyond the agentskills.io core set; the spec itself could not be fetched under the ground rules, so the characterization rests on OpenClaw's own docs treating homepage/user-invocable/command-* as OpenClaw extensions (docs/tools/skills.md#L255-L286).

### OC-ACP-1 — confirmed

**Claim (safe to cite):** All three surfaces from the issue-83021 close are in-tree at the pinned SHA, and OpenClaw supports BOTH ACP roles. (a) Client role (driving external ACP agents): the official acpx plugin is in-tree at extensions/acpx (package @openclaw/acpx; "Official ACP runtime backend for OpenClaw. ACPx lets OpenClaw run external coding harnesses through the Agent Client Protocol"); it defaults to the Claude adapter @agentclientprotocol/claude-agent-acp@0.39.0 and accepts arbitrary external ACP agents via config agents.<id>.{command,args}. The /acp chat command (spawn/steer/cancel/close/set-mode/set/cwd/permissions/timeout/model) is core at src/auto-reply/reply/commands-acp/, and sessions_spawn accepts runtime:"acp" in src/agents/tools/sessions-spawn-tool.ts. (b) Agent/server role (being driven by an editor): `openclaw acp` (src/cli/acp-cli.ts) runs an ACP bridge over stdio, serving an AgentSideConnection from @agentclientprotocol/sdk backed by the Gateway WebSocket; AcpGatewayAgent implements the Agent interface with JSON-RPC methods initialize, authenticate, session/new, session/load, session/list, session/resume, session/close, session/set_mode, session/prompt, session/cancel, plus session/update notifications. Advertised agent capabilities: loadSession:true; prompt image+embeddedContext (audio:false); mcpCapabilities http/sse:false; sessionCapabilities list/resume/close. There is also `openclaw acp client`, an interactive ACP client for testing against the bridge. acpx does NOT live in a separate repo.

**Evidence:** openclaw@203a896:extensions/acpx/README.md#L1-L5 ("Official ACP runtime backend for OpenClaw... through the Agent Client Protocol"); extensions/acpx/index.ts#L10-L21 (id "acpx", registerService); extensions/acpx/package.json#L11 ("@agentclientprotocol/claude-agent-acp": "0.39.0"); extensions/acpx/src/config-schema.ts#L46,L125 (agents?: Record<string,{command,args}>); src/auto-reply/reply/commands-acp/shared.ts#L16 (export const COMMAND = "/acp"); src/agents/tools/sessions-spawn-tool.ts#L312 (const runtime = params.runtime === "acp" ? "acp" : "subagent"); src/cli/acp-cli.ts#L14 (program.command("acp").description("Run an ACP bridge backed by the Gateway")); src/acp/server.ts#L41,L163 ("Starts the ACP Gateway bridge and serves AgentSideConnection over stdio"); src/acp/translator.ts#L250 (class AcpGatewayAgent implements Agent) with methods at L370 initialize, L397 newSession, L429 loadSession, L490 listSessions, L547 resumeSession, L585 closeSession, L596 authenticate, L600 setSessionMode, L664 prompt, L819 cancel; capabilities at translator.ts#L373-L394; wire-method names per src/acp/protocol-schema.test.ts#L26-L124 (initialize, session/new, session/prompt, session/update, session/list, session/load, session/resume, session/close). Issue metadata: .context/standards/evidence/openclaw-issue-83021.json (closed 2026-05-17) and -comments.json (clawsweeper[bot]: "shipped v2026.5.12 release already support ACP... official acpx runtime plugin, /acp controls, and sessions_spawn({ runtime: \"acp\" })"). CHANGELOG.md#L4273 has the 2026.5.12 section with ACP entries (acp.fallbacks #69542).

**Note:** Richer than the prior belief: not only can OpenClaw drive external ACP agents (acpx client role), it also exposes itself AS an ACP agent to editors via `openclaw acp` — the desk question 'what does acp-cli.ts actually expose' resolves to the full agent-side method set listed above. The bridge requires a running Gateway (it translates ACP to Gateway sessions rather than embedding the agent loop).

### OC-A2A-1 — confirmed

**Claim (safe to cite):** OpenClaw core has no native A2A-protocol (a2a-protocol.org) support: no Agent Card, no /.well-known/agent-card.json (or agent.json) endpoint, and no A2A JSON-RPC surface anywhere in src/, packages/, or extensions/. Every "a2a"/"A2A" occurrence in the tree refers to OpenClaw's internal same-gateway agent-to-agent messaging (the sessions_send bounded ping-pong flow governed by the tools.agentToAgent config), which is unrelated to the A2A protocol; "a2ui" hits belong to the canvas A2UI surface, a different protocol.

**Evidence:** Commands run in the pinned checkout: (1) `grep -rni "agent-card\|agentcard" src/ packages/ extensions/` — returned nothing. (2) `rg -il '\ba2a\b' src/ -g '!*.test.ts' -g '!*test*'` — only src/agents/tools/sessions-send-tool.ts, session-status-tool.ts, sessions-send-helpers.ts; `rg -il '\ba2a\b' packages/` — only packages/acp-core/src/session-interaction-mode.ts (comment about "skipping the A2A ping-pong flow in `sessions_send`"); `rg -il '\ba2a\b' extensions/` — only extensions/memory-core/src/session-search-visibility.test.ts (agentToAgent visibility tests). These are the internal flow: openclaw@203a896:src/agents/tools/sessions-send-tool.a2a.ts#L1-L5 ("sessions_send agent-to-agent reply flow. Runs bounded ping-pong delivery...") and src/config/types.tools.ts#L690 (agentToAgent?: {...}). (3) `grep -rni "well-known\|well_known" src/ packages/ extensions/` — only MCP OAuth discovery (src/gateway/mcp-http.request.ts#L148 handles GET /.well-known/ for oauth-protected-resource) plus unrelated comments/test vectors; no agent-card route. (4) `grep -rni "a2a-protocol" src/ packages/ extensions/` — nothing. Case-insensitive non-word-boundary grep also hit a base64 WAV fixture in extensions/qa-matrix (substring noise) and A2UI (canvas) strings — both excluded as non-protocol hits.

**Note:** Matches the maintainer close on #6842 (evidence JSON: steipete 2026-04-26: "Current main does not implement native A2A protocol endpoints") and the community-clarified distinction in that thread that tools.agentToAgent is internal routing, not the A2A protocol. Community plugins (openclaw-a2a-gateway, @a2anet/openclaw-a2a-plugin, ClawNexus) exist outside this repo per the issue comments — none are vendored in-tree.

### OC-PLUGIN-1 — confirmed

**Claim (safe to cite):** The plugin API supports both agent tools and gateway HTTP routes, which is exactly the surface Peter's #6842 close relies on for a community A2A plugin. api.registerTool is documented in docs/plugins/building-plugins.md (with the manifest contracts.tools requirement); api.registerHttpRoute is documented in docs/plugins/architecture-internals.md ("Plugins can expose HTTP endpoints with api.registerHttpRoute(...)", with path/auth/match/handler fields) and appears in the sdk-overview.md capability table as "Gateway HTTP endpoint". Both are typed on the plugin SDK API object: registerTool(tool, opts?) and registerHttpRoute(params: OpenClawPluginHttpRouteParams {path, handler, handleUpgrade?, auth, match?, ...}).

**Evidence:** openclaw@203a896:docs/plugins/building-plugins.md#L131,L203,L217 ("api.registerTool({ name: \"my_tool\", ... })", "Every tool registered with `api.registerTool(...)` must also be declared in the plugin manifest"); docs/plugins/architecture-internals.md#L622-L635 ("Plugins can expose HTTP endpoints with `api.registerHttpRoute(...)`" + example webhook route with auth: "plugin"); docs/plugins/sdk-overview.md#L167 ("| `api.registerHttpRoute(params)` | Gateway HTTP endpoint |"); SDK types: src/plugins/types.ts#L2645-L2654 (registerTool, registerHttpRoute) and #L2139-L2151 (OpenClawPluginHttpRouteParams). Runtime wiring: src/plugins/registry.ts#L821,L2810.

**Note:** One precision vs the desk framing: at this SHA, HTTP-route registration is documented in architecture-internals.md and the sdk-overview.md capability table, not in building-plugins.md itself (building-plugins.md covers tools; steipete's close-comment reference to a 'plugin capability table' with agent tools + HTTP routes maps to sdk-overview.md at this SHA). The plugin API also offers registerGatewayMethod (gateway RPC) and registerCli, further supporting an out-of-tree A2A implementation.

### OC-META-1 — corrected

**Claim (safe to cite):** VISION.md states, under "## Plugins & Memory" at lines 63-66 (not ~54 at this SHA): "OpenClaw has an extensive plugin API. / Core stays lean; optional capabilities should usually ship as plugins. / We are generally slimming down core while expanding what plugins can do. / If a useful feature cannot be built as a plugin yet, we welcome PRs and design discussions that extend the plugin API instead of adding one-off core behavior." Line 79 adds: "The bar for adding optional plugins to core is intentionally high."

**Evidence:** openclaw@203a896:VISION.md#L63-L66 — verbatim: "OpenClaw has an extensive plugin API.\nCore stays lean; optional capabilities should usually ship as plugins.\nWe are generally slimming down core while expanding what plugins can do." And VISION.md#L79: "The bar for adding optional plugins to core is intentionally high."

**Note:** Only the line number needed correcting: steipete's #6842 close cited VISION.md:54 at commit 0bcb4c95c185; at the pinned SHA 203a896 the lean-core text sits at lines 63-66 (the wording itself is intact). Cite lines 63-66 (and 79) in the doc, not 54.

## Harness: Claude Code subagent (Fable 5) — read-only verification against pinned checkout nanoclaw@aecad864e6371cb2a77ceaff8a38f9c4a8b71774 at .context/standards/checkouts/nanoclaw (rg/grep/find/git ls-tree only; no fetches, no modifications)
### NC-SKILLS-1 — corrected

**Claim (safe to cite):** NanoClaw has no skill-loader/frontmatter parser of its own. Host-side discovery is directory-name-only: container/skills/<name>/ dirs are mounted RO at /app/skills, and per-group symlinks in data/v2-sessions/<agent-group-id>/.claude-shared/skills/<name> -> /app/skills/<name> (synced per the container.json `skills` selection, default 'all' = every dir) are mounted at /home/node/.claude, where Claude Code / the Agent SDK inside the container reads ~/.claude/skills and does ALL frontmatter interpretation. So 'name + description only' is wrong: the honored field set is whatever the embedded Claude Code runtime honors — in-tree skills actively use name, description, and allowed-tools, and the repo's own docs say to follow the Claude Code skills standard for 'all available frontmatter fields'. Unknown/optional fields (license, compatibility, metadata) are silently ignored — no error, no warning — because NanoClaw never opens SKILL.md; the shipped default skill onecli-gateway carries `compatibility:` and `metadata:` keys and works. Skills also land per-group via templates (real dirs stamped into .claude-shared/skills) and are copied host-side into other providers' skill dirs (e.g. codex reads .agents/skills); operator-facing skills live in .claude/skills/ on main for the operator's own Claude Code session, not loaded by NanoClaw code.

**Evidence:** nanoclaw@aecad86:src/container-runner.ts#L346-349 ("Shared skills — read-only, symlinks in .claude-shared/skills/ point here" → mount /app/skills), #L367-408 (syncSkillSymlinks: symlink targets `/app/skills/${skill}`), #L414-426 (selectedSkillNames: 'all' = fs.readdirSync(container/skills) dirs), #L338-340 (.claude-shared mounted at /home/node/.claude); nanoclaw@aecad86:src/group-skills.ts#L4-8 ("Claude reads that store directly — it is mounted at ~/.claude/skills") and #L13-14 (codex → .agents/skills); nanoclaw@aecad86:CONTRIBUTING.md#L110-126 ("All skills use the Claude Code skills standard", name/description rules, "See the skills standard for all available frontmatter fields") and #L101 ("Use `allowed-tools` frontmatter to scope tool permissions"); nanoclaw@aecad86:container/skills/onecli-gateway/SKILL.md#L10-13 (`compatibility: Requires HTTPS_PROXY...`, `metadata:\n author: onecli\n version: "0.5.0"`); nanoclaw@aecad86:container/skills/agent-browser/SKILL.md#L4 (`allowed-tools: Bash(agent-browser:*)`); nanoclaw@aecad86:container/agent-runner/src/providers/claude.ts#L60 ('Skill' in TOOL_ALLOWLIST) and #L418 (settingSources: ['project','user','local']); nanoclaw@aecad86:src/templates/create-agent.ts#L70-74 (template skills cpSync'd opaquely into .claude-shared/skills). ABSENCE of any in-repo frontmatter parser: `rg -ni "frontmatter|front-matter|gray.?matter|js-yaml|parseYaml|yaml\.parse" --glob '!.git' --glob '!pnpm-lock.yaml' --glob '!*.lock' -l` → only CONTRIBUTING.md (docs); `rg -n "SKILL\.md" src/ setup/ scripts/ container/agent-runner/src/ --glob '*.ts'` (non-comment hits) → only path strings handed to the operator's Claude session and test fixtures copied verbatim; `rg -ni "license" src/ container/agent-runner/src/ setup/ scripts/ --glob '*.ts'` → no frontmatter-related hits.

**Note:** Desk research said 'name + description only honored'. Corrected: NanoClaw core parses zero frontmatter fields; discovery is by directory name, and frontmatter semantics are delegated entirely to Claude Code/Agent SDK in-container (name, description, allowed-tools demonstrably in use; unknown fields ignored silently, never warned or rejected). Non-Claude surfaces-owning providers (codex/opencode from the providers branch) read their own per-group skill dirs populated by host-side copy.

### NC-SKILLS-2 — corrected

**Claim (safe to cite):** Channel and provider code is distributed from two long-lived registry branches, `channels` and `providers` (both present as origin refs in the clone), via an additive per-file fetch: `git fetch origin <branch>` then `git show origin/<branch>:<path> > <path>` for each file, explicitly never `git merge`. The copies are path-preserving (each file lands at its canonical repo path, e.g. src/channels/discord.ts, plus its registration test), but it is NOT a recursive directory fetch and it does NOT distribute skill directories: the SKILL.md must enumerate every file, and the skill folders themselves (.claude/skills/<name>/SKILL.md + REMOVE.md, and utility skills' scripts/) ship on main, while container skills (SKILL.md + scripts) ship in container/skills/ on main. What lives on the branches is the adapter/provider source, tests, and a few extra skill docs — not the mechanism by which SKILL.md+scripts layouts reach an install.

**Evidence:** nanoclaw@aecad86:CONTRIBUTING.md#L41-49 ("code lives on a long-lived registry branch (`channels` or `providers`)"; "copies each file in with `git show origin/channels:<path> > <path>`. Install is an additive fetch, never a `git merge`"); nanoclaw@aecad86:docs/skills-model.md#L69-71 ("That code lives on a long-lived registry branch (`channels`, `providers`) ... the skill fetches it from there (`git show origin/channels:path > path`)"; "This fetch is additive, never a merge"); nanoclaw@aecad86:.claude/skills/add-discord/SKILL.md#L28-35 (fetch + two `git show` lines to identical paths); nanoclaw@aecad86:.claude/skills/add-opencode/SKILL.md#L41-52 (7 per-file `git show origin/providers:...` lines spanning host and container trees); `git branch -a` in the clone lists remotes/origin/channels and remotes/origin/providers; `git ls-tree -r --name-only origin/channels` vs main shows the branch adds src/channels/*.ts adapters + *-registration.test.ts (and some extra .claude/skills docs), confirming code-at-canonical-paths, not skill-dir bundles; nanoclaw@aecad86:CLAUDE.md#L116-119 ("Trunk does not ship any specific channel adapter... adapters and providers live on long-lived sibling branches and get copied in by skills").

**Note:** Mechanism confirmed exactly as researched; the correction is the layout framing: registry branches distribute adapter/provider code files one-by-one to preserved repo paths (per an explicit list in the SKILL.md), whereas the SKILL.md+scripts/ skill-directory layout is carried on main (operator skills in .claude/skills/, container skills in container/skills/, template skills stamped whole-directory by cpSync). A multi-file fetch stays layout-correct only because the SKILL.md lists every file.

### NC-ACP-1 — confirmed

**Claim (safe to cite):** NanoClaw contains no reference to ACP / the Agent Client Protocol anywhere in the tree: no protocol code, no dependency, no docs mention.

**Evidence:** Commands run at nanoclaw@aecad86 (repo root): (1) `grep -rni "agent-client-protocol\|agentclientprotocol" . --exclude-dir=.git` → no matches (exit 1). (2) `grep -rni "\bacp\b" . --exclude-dir=.git` → no matches (exit 1). (3) `grep -rni "acp" . --exclude-dir=.git -l` → only pnpm-lock.yaml and container/agent-runner/bun.lock; inspecting the matches shows they are all 'acp'/'ACP' substrings inside base64 sha512 integrity hashes (e.g. pnpm-lock.yaml:1101 "...2UclACPUUEqlt...") — classified as false positives. No @agentclientprotocol/* or similar package appears in package.json, container/agent-runner/package.json, or either lockfile's dependency names.

**Note:** Absence is clean. The container agent's editor-protocol surface is the Claude Agent SDK invoked in-process (container/agent-runner/src/providers/claude.ts), not ACP.

### NC-A2A-1 — confirmed

**Claim (safe to cite):** NanoClaw contains no reference to the A2A / Agent2Agent interop protocol: no agent cards, no /.well-known/agent.json, no A2A JSON-RPC. The string 'a2a' does appear in 15 files, but every hit is NanoClaw's internal 'agent-to-agent' module — inter-agent messaging between agent groups inside one install, routed through the same SQLite session DBs (outbound rows with channel_type='agent' copied into the target group's inbound.db) — which is unrelated to the A2A protocol.

**Evidence:** Commands run at nanoclaw@aecad86: (1) `grep -rni "agent2agent\|agent-card\|agentcard" . --exclude-dir=.git` → no matches (exit 1). (2) `grep -rni "well-known\|well_known" . --exclude-dir=.git` → one match: .claude/skills/migrate-nanoclaw/SKILL.md:265 ("well-known patterns" — ordinary English, not a well-known URI). (3) `grep -rni "a2a" . --exclude-dir=.git -l` → 15 files, all internal: nanoclaw@aecad86:src/modules/agent-to-agent/index.ts#L1-2 ("Agent-to-agent module — inter-agent messaging and on-demand agent creation"), nanoclaw@aecad86:src/modules/agent-to-agent/agent-route.ts#L1-9 ("Outbound messages with `channel_type === 'agent'` target another agent group rather than a channel ... Content is copied into the target's inbound DB"), plus a2a return-path columns/tests (src/db/schema.ts#L179-184, src/db/session-db.ts#L334-370).

**Note:** Important false-positive warning for the gap-analysis doc: NanoClaw branch names (feat/a2a-approval-policies, skill/a2a-return-path) and code comments use 'a2a' to mean their in-process agent-to-agent module. Do not cite these as A2A-protocol adoption; there is none.

### NC-ARCH-1 — confirmed

**Claim (safe to cite):** Sidecar-relevant architecture facts, all confirmed: (a) Per-session SQLite is the sole IO boundary — 'Everything is a message... The two session DBs are the sole IO surface.' Each session has inbound.db (host-written: messages_in, delivered, destinations, session_routing) and outbound.db (container-written: messages_out, processing_ack, session_state, container_state); schemas are the INBOUND_SCHEMA/OUTBOUND_SCHEMA constants applied by ensureSchema() at session-folder init. (b) ChannelAdapters are in-process: a module calls registerChannelAdapter(name, {factory, containerConfig}) at import time and the barrel src/channels/index.ts (trunk ships only `import './cli.js'`) triggers it; initChannelAdapters() instantiates all registered adapters at host startup; the adapter contract is the ChannelAdapter interface (setup/teardown/deliver/isConnected + optional setTyping/openDM/subscribe). There is no out-of-process adapter registration; the nearest external ingress is the lazy HTTP webhook server (0.0.0.0:3000 by default) whose routes are still registered in-process. (c) An external process CAN mechanically open the DBs — they are plain SQLite files at data/v2-sessions/<agent_group_id>/<session_id>/{inbound.db,outbound.db} — but the design explicitly forbids WAL (journal_mode=DELETE is load-bearing for cross-mount visibility) and documents three invariants: exactly one writer per file ('concurrent writers corrupt the DB' — the host is inbound.db's sole writer, so a sidecar writing messages_in while the host runs violates it), open-write-close per operation, and seq parity (host writes even seq via nextEvenSeq, container odd). Reading messages_out externally is read-only-safe (the host itself opens outbound.db readonly) but would race the host's delivery poll for the same rows; a conformant sidecar should therefore enter via a registered ChannelAdapter (one appended barrel import — the sanctioned skill-shaped reach-in) rather than raw DB writes. (d) Core size: host src/ = 15,945 lines of TypeScript in 131 non-test files (25,919 lines / 180 files with tests); container agent-runner src/ = 4,491 lines in 32 non-test files (6,335 with tests); combined non-test core ≈ 20.4k lines, plus a ~14.4k-line setup/ wizard tree outside the runtime core.

**Evidence:** (a) nanoclaw@aecad86:CLAUDE.md#L24 ("The two session DBs are the sole IO surface."); nanoclaw@aecad86:src/db/schema.ts#L162-188 (CREATE TABLE messages_in) and #L227-240 (CREATE TABLE messages_out); nanoclaw@aecad86:src/db/session-db.ts#L13-18 (ensureSchema applies them with `db.pragma('journal_mode = DELETE')`); nanoclaw@aecad86:src/session-manager.ts#L131-139 (initSessionFolder). (b) nanoclaw@aecad86:src/channels/channel-registry.ts#L26-28 ("registerChannelAdapter... Called by channel modules on import") and #L124-173 (initChannelAdapters); nanoclaw@aecad86:src/channels/index.ts#L1-9 (self-registration barrel, only cli in trunk); nanoclaw@aecad86:src/channels/adapter.ts#L113-179 (ChannelAdapter interface); nanoclaw@aecad86:src/webhook-server.ts#L1-19,#L162 (lazy webhook server, listens 0.0.0.0). (c) nanoclaw@aecad86:src/session-manager.ts#L5-11 ("1. journal_mode=DELETE — WAL's mmapped -shm doesn't refresh host→guest... 3. One writer per file — ... concurrent writers corrupt the DB"); nanoclaw@aecad86:docs/db.md#L22 ("Single-writer rule. Every SQLite file has exactly one writer. Host writes... every inbound.db; container writes only its own outbound.db"); nanoclaw@aecad86:container/agent-runner/src/db/connection.ts#L12-18 (WAL "would leave this reader frozen on an early snapshot"); nanoclaw@aecad86:src/db/session-db.ts#L29-33 (host opens outbound.db readonly) and #L82-92 (nextEvenSeq host-even invariant); paths: src/session-manager.ts#L43-60. (d) `find src -name '*.ts' ! -name '*.test.ts' -exec cat {} + | wc -l` → 15945 (131 files; 25919/180 with tests); same over container/agent-runner/src → 4491 (32 files; 6335 with tests); setup/ non-test → 14350.

**Note:** For the sidecar proposal: the DB files are host-filesystem-local (DATA_DIR/v2-sessions/...), not WAL, busy_timeout=5000, so a read-only sidecar tailing messages_out is low-risk; an ingest sidecar must NOT write messages_in directly (second-writer corruption per the documented invariant) — it should present as a ChannelAdapter (in-process module + one barrel import, exactly the shape the skills model blesses) or feed an adapter over the existing webhook server. Also note messages wake containers via the host router/sweep, not via DB triggers — rows written behind the host's back are only picked up by the 60s host-sweep due-message scan.

### NC-PERM-1 — confirmed

**Claim (safe to cite):** There is no per-tool-call permission gate for the containerized agent: the Claude provider launches the SDK with permissionMode: 'bypassPermissions' and allowDangerouslySkipPermissions: true, so ordinary tool use (Bash, Write, WebFetch, etc.) never pauses for a human — the container's isolation (mounts, egress lockdown, OneCLI credential proxy) is the control instead. That is the ACP-mapping finding. However, NanoClaw DOES have a durable human-approval subsystem at coarser granularity: (1) a generic primitive — requestApproval() persists a pending_approvals row and DMs an Approve / Reject / Reject-with-reason card to an approver resolved from user_roles (scoped admins → global admins → owners; no env-var list), with registerApprovalHandler(action, cb) dispatching on approve; (2) consumers today: agent-initiated self-modification (install_packages / add_mcp_server MCP tools are fire-and-forget and 'Requires admin approval'), create_agent from confined groups, the a2a message gate, and unknown-sender approval (pending_sender_approvals); (3) a blocking OneCLI credential-approval bridge — the gateway holds the credentialed HTTP request open while the host DMs an approval card and waits on a promise resolved by the admin's click or expiry — which is the closest existing analog to ACP session/request_permission semantics; and (4) an ask_user_question MCP tool that blocks the agent on a real human answer via the message DBs (the SDK's native AskUserQuestion is disallowed because it returns a placeholder).

**Evidence:** nanoclaw@aecad86:container/agent-runner/src/providers/claude.ts#L416-417 ("permissionMode: 'bypassPermissions', allowDangerouslySkipPermissions: true") and #L26-36 (AskUserQuestion in SDK_DISALLOWED_TOOLS: "SDK returns a placeholder instead of blocking on a real answer"); nanoclaw@aecad86:src/modules/approvals/primitive.ts#L48-52 (Approve/Reject/Reject-with-reason card options), #L133-150 (pickApprover: "admins @ that group → global admins → owners"), #L223-280 (requestApproval: createPendingApproval + card delivery to approver DM), #L72-77 (registerApprovalHandler); nanoclaw@aecad86:container/agent-runner/src/mcp-tools/self-mod.ts#L1-14 ("Both are fire-and-forget... The host processes the request (including admin approval)") and #L43-45 (tool description "Requires admin approval"); nanoclaw@aecad86:src/modules/agent-to-agent/index.ts#L4-8 (create_agent approval for confined groups); nanoclaw@aecad86:src/db/schema.ts#L136-150 (pending_sender_approvals); nanoclaw@aecad86:src/modules/approvals/onecli-approvals.ts#L1-19 ("the gateway intercepts a credentialed request that needs human approval, it holds the HTTP connection open... Wait on an in-memory Promise: resolved by the admin click... or by a local expiry timer"); nanoclaw@aecad86:container/agent-runner/src/mcp-tools/interactive.ts#L1-5 ("ask_user_question is a blocking tool call — it writes a messages_out row with a question card, then polls messages_in for the response").

**Note:** Net for the sidecar/ACP proposal: request_permission has no tool-level hook to attach to (permissions are bypassed by design inside the sandbox), but the approvals primitive (action-keyed, card-based, DB-persisted, admin-routed) is a ready-made backend for mapping coarse-grained ACP permission requests, and the OneCLI bridge already demonstrates the blocking hold-open pattern end to end.

## Harness: zeroclaw (ZeroClaw workspace repo) @ c6f49ca6ee6828a8a5866e583432f2ec978e5866, pinned checkout <zeroclaw-workspace>
### ZC-SKILLS-1 — corrected

**Claim (safe to cite):** SkillFrontmatter (crates/zeroclaw-runtime/src/skills/frontmatter.rs) defines exactly 8 top-level fields: name, description, license, author, version, category, tags, slash_options — so author/version/category/tags/slash_options are non-spec top-level keys vs agentskills.io (name/description/license/compatibility/metadata/allowed-tools). Neither `metadata:` nor `allowed-tools:` (nor `compatibility:`) is parsed anywhere in the skills code: both frontmatter readers drop unknown keys via a `_ => {}` arm. There are TWO flat parsers: the loader's parse_simple_frontmatter (mod.rs) parses only name/description/version/author/tags (+ nested slash_options via document::parse_slash_options) — it does NOT parse license or category; the document reader's assign() (document.rs) parses name/description/license/author/version/category (+ tags + slash_options). The doc comment explicitly admits the zeroclaw-labs/zeroclaw-skills registry diverges by nesting under `metadata:`. The parsers are flat (no nested mappings) but not strictly single-line: both support YAML block scalars (>-, >, |, |-) and tags block lists.

**Evidence:** zeroclaw@c6f49ca:crates/zeroclaw-runtime/src/skills/frontmatter.rs#L33-61 (struct: "pub name: String ... pub license: Option<String> ... pub author ... pub version ... pub category ... pub tags: Vec<String> ... pub slash_options: Vec<SkillSlashOption>"); #L4-9 doc comment: "We keep the shape **flat** — `license`, `author`, `version`, `category` at the top level ... The `zeroclaw-labs/zeroclaw-skills` registry nests these under a `metadata:` block; that registry is ours and follows this flat shape going forward." Loader parser: zeroclaw@c6f49ca:crates/zeroclaw-runtime/src/skills/mod.rs#L1378-1381 ("Lightweight YAML-like frontmatter parser for simple `key: value` pairs"), #L1438-1457 (match arms only "name"/"description"/"version"/"author"/"tags", then `_ => {}`), #L295-306 (SkillMarkdownMeta has no license/category), #L1466 (slash_options via document::parse_slash_options). Document reader: zeroclaw@c6f49ca:crates/zeroclaw-runtime/src/skills/document.rs#L176-186 (assign(): "name"/"description"/"license"/"author"/"version"/"category", `_ => {}`), #L79-82 ("Does not attempt nested mappings; the schema is flat by design"), #L137 (block scalars ">-" | ">" | "|" | "|-"). Absence: `rg -n '"allowed-tools"|allowed_tools|"compatibility"|"metadata"' crates/zeroclaw-runtime/src/skills/` → no matches (exit 1); repo-wide `rg -n --hidden -g '!.git' "allowed-tools" .` hits only web/src/pages/Cron.tsx (unrelated cron UI) and .claude/skills/skill-creator/scripts/quick_validate.py#L42 (spec property list in a dev script, not the runtime parser).

**Note:** Two corrections vs the prior belief: (1) 'the parser' is really two parsers, and the frontmatter.rs doc comment overstates coverage — parse_simple_frontmatter does NOT parse license/category (only document.rs does), despite the comment claiming it 'covers every field'; (2) not strictly single-line: block scalars are supported for name/description and tags/slash_options have block forms. Caveat worth flagging in the gap doc: both parsers trim key indentation on ordinary lines, so keys nested under an ignored `metadata:` block (e.g. an indented `author:`) would be accidentally read as top-level fields — spec-shaped registry SKILL.md files are not just partially ignored, they can be misparsed.

### ZC-SKILLS-2 — confirmed

**Claim (safe to cite):** The only first-party SKILL.md files in the repo are the 9 dev-harness skills under .claude/skills/ (validate targets: .claude/skills/zeroclaw/SKILL.md, .claude/skills/changelog-generation/SKILL.md, .claude/skills/github-pr-review-session/SKILL.md; also github-pr, github-issue, github-issue-triage, squash-merge, skill-creator, wit-breaking-change-check). Shipped/user-installable skills are NOT in this repo: they live in the external zeroclaw-labs/zeroclaw-skills registry, git-cloned at runtime to ~/.zeroclaw/workspace/skills-registry (plus besoeasy/open-skills for the open-skills loader, and ClawHub zip installs). dev/test-registry-skills.sh is an E2E test that installs every registry skill by bare name against that runtime clone. No SKILL.md fixture files exist under crates/**; tests generate SKILL.md content inline via fs::write.

**Evidence:** `find . -name "SKILL.md" -not -path "./.git/*" -not -path "./.context/*"` → exactly 9 paths, all .claude/skills/*/SKILL.md. Registry sync code: zeroclaw@c6f49ca:crates/zeroclaw-runtime/src/skills/mod.rs#L51-55 ("const SKILLS_REGISTRY_REPO_URL: &str = \"https://github.com/zeroclaw-labs/zeroclaw-skills\"; const SKILLS_REGISTRY_DIR_NAME: &str = \"skills-registry\";"), #L41-43 (OPEN_SKILLS_REPO_URL = "https://github.com/besoeasy/open-skills"), #L46-49 (ClawHub). zeroclaw@c6f49ca:dev/test-registry-skills.sh#L2-4 ("Installs every skill from zeroclaw-labs/zeroclaw-skills by bare name"), #L49-50 (SKILLS_DIR=$HOME/.zeroclaw/workspace/skills; REGISTRY_DIR=$HOME/.zeroclaw/workspace/skills-registry). Fixture absence: `find crates -path "*skills*" -name "*.md" -not -path "*/target/*"` → only crates/zeroclaw-runtime/src/skills/review_prompt.md (a prompt, not a skill); inline-fixture example zeroclaw@c6f49ca:crates/zeroclaw-gateway/src/a2a.rs#L626-636 (write_skill() writes "---\nname: {display}\ndescription: ...\n---").

**Note:** .context/standards/checkouts/** contains SKILL.md files from third-party checkouts (nanoclaw) — not ZeroClaw first-party; excluded. The .claude/skills frontmatter observed uses only name/description (spec-clean), so they are safe skills-ref validate targets; the registry's own SKILL.md files (with the nested metadata: block per frontmatter.rs doc) are in a different repo and cannot be validated from this checkout.

### ZC-ACP-1 — confirmed

**Claim (safe to cite):** The ACP server (crates/zeroclaw-channels/src/orchestrator/acp_server.rs) handles exactly these JSON-RPC methods: initialize, session/new, session/load, session/resume, session/close, session/prompt, session/stop, session/cancel, and session/event|session/update (as notifications); anything else returns METHOD_NOT_FOUND. It advertises "protocolVersion": 1 (ACP_PROTOCOL_VERSION = 1), agentInfo name "zeroclaw-acp", authMethods: [], and a _meta.zeroclaw extension object with fields maxSessions, sessionTimeoutSecs, and optional defaultModel. The stdio path is src/bin/zeroclaw-acp-bridge.rs, a plain stdin/stdout-to-WebSocket relay that connects to the gateway's /acp endpoint (ws(s)://host:port[/prefix]/acp) — NOT a self-contained native stdio agent binary — and it is gated on gateway pairing: require_pairing defaults true, and without a cached token, ZEROCLAW_ACP_BRIDGE_TOKEN, or a --pair-code exchange at POST /pair it aborts with a pairing error; the gateway /acp handler also returns 401 without a paired token.

**Evidence:** Methods: zeroclaw@c6f49ca:crates/zeroclaw-channels/src/orchestrator/acp_server.rs#L398-422 ("\"initialize\" => ... \"session/new\" => ... \"session/load\" ... \"session/resume\" ... \"session/close\" ... \"session/prompt\" ... \"session/stop\" ... \"session/cancel\" ... \"session/event\" | \"session/update\" ... _ => ... code: METHOD_NOT_FOUND"). Version + _meta: #L478-502 ("\"protocolVersion\": ACP_PROTOCOL_VERSION ... \"agentInfo\": { \"name\": \"zeroclaw-acp\" ... } ... \"authMethods\": [], \"_meta\": { \"zeroclaw\": zeroclaw_meta }"), #L464-470 (zeroclaw_meta = maxSessions, sessionTimeoutSecs, optional defaultModel); zeroclaw@c6f49ca:crates/zeroclaw-api/src/jsonrpc.rs#L121 ("pub const ACP_PROTOCOL_VERSION: u64 = 1;"). Bridge: zeroclaw@c6f49ca:src/bin/zeroclaw-acp-bridge.rs#L17 ("ERROR: Gateway pairing is active but no ACP bridge token is cached. Run `zeroclaw gateway get-paircode --new` ..."), #L126-153 (resolve_acp_bridge_target: require_pairing → env token → cached token → pair-code exchange, else bail), #L359-363 (acp_websocket_url → "{scheme}://{host}:{port}{path_prefix}/acp"), #L439-441 (default_require_pairing → true). Gateway side: zeroclaw@c6f49ca:crates/zeroclaw-gateway/src/lib.rs#L2015 (.route("/acp", get(acp::handle_ws_acp))); crates/zeroclaw-gateway/src/acp.rs#L32-40 (401 "Unauthorized - provide Authorization header..." when pairing required).

**Note:** initialize also reports agentCapabilities: promptCapabilities image/audio/embeddedContext all false, mcpCapabilities http/sse false, loadSession + sessionCapabilities {resume, close} only when the session store opened (acp_server.rs#L472-492). The server additionally EMITS session/update, session/request_permission, and elicitation/create toward the client (acp_channel.rs#L8-27) — outbound, not handled methods.

### ZC-ACP-2 — confirmed

**Claim (safe to cite):** There is no ACP client in ZeroClaw: no code connects to or spawns an external ACP agent. There is no dependency on the agent-client-protocol crate, no ClientSideConnection, and no subprocess spawning that speaks ACP. The only outbound ACP-adjacent connection is zeroclaw-acp-bridge, which dials ZeroClaw's OWN gateway WebSocket, not a third-party agent.

**Evidence:** Commands run at zeroclaw@c6f49ca, all returning nothing: (1) `rg -n "ClientSideConnection" crates/ src/ apps/` → exit 1, 0 matches. (2) `rg -l "agent_client_protocol|agent-client-protocol" Cargo.toml Cargo.lock crates/` → exit 1, 0 matches. (3) `rg -n "ClientSideConnection|acp_client|AcpClient|client_side|spawn.*acp|connect.*acp" crates/ src/ --ignore-case` (excluding acp_server.rs/acp_channel.rs/acp-bridge) → only 3 non-client hits: crates/zeroclaw-runtime/src/subagent/mod.rs#L143 (doc comment), crates/zeroclaw-runtime/src/rpc/dispatch.rs#L1026 (spawn_blocking for session-store IO), crates/zeroclaw-config/src/schema.rs#L3505 (comment). (4) `rg -n '"session/prompt"|"session/new"' crates/ src/ -g '!*/tests/*'` → only server-side: acp_server.rs#L400,#L404 (handlers) and rpc/dispatch.rs#L157-159 (the runtime's own inbound RPC method table). (5) `rg -n "Command::new" crates/zeroclaw-channels/src/orchestrator/acp_server.rs src/bin/zeroclaw-acp-bridge.rs` → 0 matches.

**Note:** The bridge (src/bin/zeroclaw-acp-bridge.rs#L42-44) is client-side WebSocket plumbing to ZeroClaw's own gateway /acp endpoint — it does not make ZeroClaw a client of external ACP agents.

### ZC-A2A-1 — confirmed

**Claim (safe to cite):** The A2A server surface (crates/zeroclaw-gateway/src/a2a.rs) is message/send-only: any other method on POST /a2a/{alias} returns JSON-RPC -32601 ("only message/send is supported on this build"). There is no task store: each message/send runs one synchronous agent turn and returns a freshly-minted Task (new UUID id, state "completed") — no tasks/get, tasks/cancel, message/stream, or SSE/streaming exists anywhere for A2A. Cards: per-alias spec path GET /a2a/{alias}/.well-known/agent-card.json, plus a deliberately non-spec catalog at /.well-known/agents-card.json (and prefixed alias /a2a/.well-known/agents-card.json); routes are registered in gateway lib.rs behind #[cfg(feature = "a2a")] (default-on), with the task POST on the long-running-timeout sub-router. The task POST requires the gateway pairing bearer token (require_auth before any gating); the discovery GETs are intentionally unauthenticated. The cards honestly advertise capabilities streaming:false, pushNotifications:false, extendedAgentCard:false.

**Evidence:** zeroclaw@c6f49ca:crates/zeroclaw-gateway/src/a2a.rs#L492-502 ("if req.method != \"message/send\" { ... -32601, \"Method not found: only message/send is supported on this build\" }"); #L540-557 pre-completed task, no store ("let session_id = format!(\"a2a_{alias}_{}\", Uuid::new_v4()); ... OutTask { id: Uuid::new_v4().to_string(), ... status: OutTaskStatus { state: \"completed\".to_string() } ... }"); #L446-448 bearer gate ("if let Err(e) = require_auth(&state, &headers) { return e.into_response(); }") with #L437-439 doc ("Requires a paired bearer token (a turn is tool-enabled, so it is never served unauthenticated)"); card paths #L45-58 ("Deliberately NOT the spec's singular `agent-card.json` ... CATALOG_CARD_PATH: &str = \"/.well-known/agents-card.json\" ... CATALOG_CARD_PREFIXED_PATH ... WELL_KNOWN_AGENT_CARD_PATH: &str = \"/.well-known/agent-card.json\""); route registration #L587-595 (a2a_routes: catalog x2 + "/a2a/{{alias}}{WELL_KNOWN_AGENT_CARD_PATH}") and #L602-604 (a2a_task_route: post /a2a/{alias}); zeroclaw@c6f49ca:crates/zeroclaw-gateway/src/lib.rs#L1972-1973 ("#[cfg(feature = \"a2a\")] let inner = inner.merge(a2a::a2a_routes());") and #L2039-2040 (task route merged into long_running_router); capability flags #L208-212 ("streaming: Some(false), push_notifications: Some(false), extended_agent_card: Some(false)") with test #L727 (assert_eq!(json["capabilities"]["streaming"], false)). Absence of other methods: `rg -n "message/stream" crates/ src/ docs/` → exit 1; `rg -n "tasks/get" crates/ src/ docs/` → exit 1.

**Note:** The catalog card additionally aggregates every published alias's skills with alias-prefixed ids and alias tags (#L156-163) — a second non-spec behavior beyond the plural path. handle_alias_task checks auth BEFORE the enabled/published gates (#L446-463), so an unauthenticated probe gets 401 even when A2A is disabled. Docs (docs/book/src/gateway/a2a-discovery.md#L289-292) match the code: -32601 for other methods, -32602 for empty message, HTTP 400 for non-JSON-RPC bodies.

### ZC-A2A-2 — confirmed

**Claim (safe to cite):** There is no A2A client in ZeroClaw: no code fetches a remote agent card or issues an outbound message/send to another deployment. All A2A code lives in the gateway (server) and config crates; the config wrapper explicitly reserves 'outbound client config' as future work.

**Evidence:** Commands run at zeroclaw@c6f49ca, with full results: (1) `rg -n "message/send" crates/ src/` → only crates/zeroclaw-gateway/src/a2a.rs (server handler), crates/zeroclaw-gateway/src/api.rs#L4224 (test request body), crates/zeroclaw-gateway/src/openapi.rs#L333 (endpoint description string), crates/zeroclaw-channels/src/mochat.rs#L113 (unrelated MoChat REST URL "{}/api/message/send"). (2) `rg -n "agent-card.json|agent_card" crates/ src/ --glob '!*/tests/*' -l` → only crates/zeroclaw-gateway/src/api.rs, crates/zeroclaw-gateway/src/a2a.rs, crates/zeroclaw-gateway/tests/fixtures/a2a-v1.proto. (3) `rg -li "a2a" crates/zeroclaw-runtime/src/ crates/zeroclaw-tools/src/ crates/zeroclaw-providers/src/ src/` → exit 1, 0 matches (no A2A code outside gateway+config crates). (4) `rg -n "well-known" -g '*.rs' .` (excluding target/.git/.context) → no card-fetch client; all hits are the gateway's own serving constants or unrelated (uf2 mount points, nevis JWKS, xAI OAuth discovery). Future-work marker: zeroclaw@c6f49ca:crates/zeroclaw-config/src/multi_agent.rs#L198-200 ("the `a2a` table can host sibling sub-tables (outbound client config, signing) in later slices").

**Note:** docs/book/src/gateway/a2a-discovery.md#L392-414 describes multi-deployment discovery purely from the standpoint of an external HTTP client (curl) — consistent with no in-product client.

### ZC-CFG-1 — confirmed

**Claim (safe to cite):** Config gates are default-closed exactly as believed: [a2a.server] enabled defaults to false (plus bind=None, port=None advertise-only overrides and public_base_url=""); per-agent [agents.<alias>.a2a] published defaults to false and exposed_skills defaults to empty (empty = advertise no skills) — all in crates/zeroclaw-config/src/multi_agent.rs, each locked by a unit test. The [acp] section (crates/zeroclaw-config/src/schema.rs, AcpConfig) has NO enabled flag — only default_agent (default None), max_sessions (default 10), session_timeout_secs (default 3600); the gateway /acp endpoint is always mounted and gated by pairing auth instead.

**Evidence:** zeroclaw@c6f49ca:crates/zeroclaw-config/src/multi_agent.rs#L176-179 ("pub struct A2aServerConfig { /// Master switch for the inbound A2A surface. Default `false`: no well-known route, no per-alias cards, no inbound endpoints. pub enabled: bool"), #L230-239 ("pub published: bool" — "Default `false`: the alias is excluded from the discovery catalog"; "pub exposed_skills: Vec<String>" — "Empty = no skills advertised"); tests #L430-436 ("let cfg = A2aServerConfig::default(); assert!(!cfg.enabled);") and #L453-458 ("assert!(!cfg.published); assert!(cfg.exposed_skills.is_empty());"). zeroclaw@c6f49ca:crates/zeroclaw-config/src/schema.rs#L11726-11757 (AcpConfig: default_agent Option<String>, "Maximum number of concurrent ACP sessions. Default: `10`", "Idle session timeout in seconds ... Default: `3600`"; fn default_acp_max_sessions() -> usize { 10 }; fn default_acp_session_timeout_secs() -> u64 { 3600 }).

**Note:** For honest what-to-flip docs: ACP needs no config flip (endpoint always on, pairing-gated); A2A needs two flips plus a skill list — a2a.server.enabled, agents.<alias>.a2a.published, and agents.<alias>.a2a.exposed_skills (empty list advertises zero skills even when published). bind/port under [a2a.server] are advertise-only (nothing binds there; routes serve on the gateway listener — multi_agent.rs#L180-189).

### ZC-GW-1 — confirmed

**Claim (safe to cite):** For a local TCK/probe run: the gateway is served by the single `zeroclaw` binary (`zeroclaw gateway` subcommand → run_gateway), defaulting to 127.0.0.1:42617 with require_pairing=true. Get a bearer token via the paircode flow: `zeroclaw gateway get-paircode` (or --new) prints a code; exchange it with POST /pair carrying header X-Pairing-Code, which returns {token} used as Authorization: Bearer on the message/send POST (discovery card GETs need no token). Minimal config to publish an agent: under [a2a.server] set enabled=true; define an [agents.<alias>] entry (enabled defaults true) with skill_bundles listing the bundle(s) that carry the skills, and under its a2a sub-table set published=true and exposed_skills naming the resolved skill ids. Default cargo features DO include the full interop surface: root default = [agent-runtime, default-channels, acp-bridge, gateway, ...], default-channels includes channel-acp-server, and the zeroclaw-gateway crate's own defaults include the a2a feature (root dep does not disable default features).

**Evidence:** Binary: zeroclaw@c6f49ca:Cargo.toml#L63-69 ("[[bin]] name = \"zeroclaw\" path = \"src/main.rs\""); src/main.rs#L3763-3772 (GatewayCommands::Start → "run_gateway_if_enabled(&host, port, config, None)"). Port/host: crates/zeroclaw-config/src/schema.rs#L6391-6396 ("Gateway port (default: 42617)" / "Gateway host (default: 127.0.0.1)" / "Require pairing before accepting requests (default: true)"), #L6492 ("42617"). Paircode: src/lib.rs#L187-191 ("zeroclaw gateway get-paircode  # show current pairing code ... --new # add another client"); crates/zeroclaw-gateway/src/lib.rs#L1775-1776 (".route(\"/pair\", post(handle_pair)) .route(\"/pair/code\", get(handle_pair_code))"); exchange shape src/bin/zeroclaw-acp-bridge.rs#L329-357 (POST with .header("X-Pairing-Code", code) → PairResponse { token }). Docs: docs/book/src/gateway/a2a-discovery.md#L17-24 ("pass a pairing-derived bearer token on the task POST: curl -X POST http://localhost:42617/a2a/agent_alpha -H \"Authorization: Bearer $ZEROCLAW_TOKEN\" ..."), #L36-44 (the two discovery GETs), #L333-357 (three default-off gates + skill_bundles requirement + a2a_exposed_skills_without_bundles warning). Features: Cargo.toml#L279-287 ("default = [\"agent-runtime\", \"default-channels\", \"acp-bridge\", \"gateway\", \"observability-prometheus\", \"schema-export\"]"), #L304-308 (default-channels includes "channel-acp-server"), #L90 (zeroclaw-gateway = { workspace = true, optional = true } — defaults not disabled); crates/zeroclaw-gateway/Cargo.toml#L85 ("default = [\"schema-export\", \"a2a\"]").

**Note:** Probe caveats: cards return 404 (not 401) until a2a.server.enabled and the alias is published; the task POST returns 401 without a token even when A2A is disabled (auth checked first). A bare GET on /a2a/{alias} serves the SPA dashboard fallback, not JSON (docs #L303-315) — a TCK hitting the interface URL with GET will see text/html 200. Skills only appear on cards if the SKILL.md resolves in a declared bundle (empty skills:[] is the documented common failure). Per repo convention, keep the config guidance as field names in prose, not embedded TOML blocks.

## Harness: multi (openclaw + nanoclaw + zeroclaw) — official Agent Skills validator skills-ref@0.1.5, empirical runs 2026-07-02
### SKV-TOOLING — confirmed

**Claim (safe to cite):** The official Agent Skills reference validator was obtained via install path (a): `npm exec --yes skills-ref@latest -- --help` succeeded on the first attempt (exit 0), resolving npm package skills-ref@0.1.5 with subcommands validate / read-properties / to-prompt; paths (b)(c)(d) were never needed. For the batch runs the same package was pinned locally with `npm install --prefix .context/standards/tools/skills-ref-install skills-ref@latest` and executed as `node .../node_modules/skills-ref/dist/cli.js validate <path>` (this sandbox drops PATH inside shell functions, so node was invoked by absolute path).

**Evidence:** Live run: `npm exec --yes skills-ref@latest -- --help` printed 'Usage: skills-ref [options] [command] / Reference library for Agent Skills / Commands: validate <skill_path> ...' with EXIT: 0; `-- --version` printed '0.1.5'. Header line recorded in every evidence file, e.g. .context/standards/evidence/skills-ref-openclaw-weather.txt#L1: '# skills-ref@0.1.5 (npm package skills-ref, obtained via: npm exec --yes skills-ref@latest)'. Local pin at .context/standards/tools/skills-ref-install/node_modules/skills-ref/dist/cli.js (shebang '#!/usr/bin/env node').

**Note:** Validator obtained without issue; no fallback to @agentskills/skills-ref, npx, or repo clone required.

### SKV-UNKNOWN-FIELDS — confirmed

**Claim (safe to cite):** skills-ref@0.1.5 HARD-FAILS (nonzero exit, validation error) on any unknown top-level frontmatter field; it has no warn-only behavior. The complete allowlist per its own error message is: allowed-tools, compatibility, description, license, metadata, name. Vendor extensions are only portable when nested under `metadata`.

**Evidence:** .context/standards/evidence/skills-ref-synthetic-unknown-field-probe.txt — probe skill (valid name+description plus `author: test`) at evidence/synthetic-skill-test/unknown-field-probe/SKILL.md: 'Validation failed for evidence/synthetic-skill-test/unknown-field-probe: / - Unexpected fields in frontmatter: author. Only allowed-tools, compatibility, description, license, metadata, name are allowed. / EXIT CODE: 1'. Control in same file (identical skill minus author): 'Valid skill: evidence/synthetic-skill-test/unknown-field-probe-control / EXIT CODE: 0'.

**Note:** Definitive answer to the critical question: FAIL, not WARN. Exit code 1 vs 0 isolated to the single bogus field via the control run.

### SKV-OPENCLAW — confirmed

**Claim (safe to cite):** OpenClaw harness-shipped skills (repo-root skills/, the richest at 4 frontmatter fields: name, description, homepage, metadata): all 3 sampled (weather, 1password, peekaboo) FAIL skills-ref with exit 1, solely because of the top-level `homepage` field; their vendor-extension payloads under `metadata.openclaw` (emoji, install specs) pass because `metadata` is allowlisted. 33 of the 52 skills in the catalog carry top-level `homepage` and would fail as-is; the other 19 (e.g. github: name/description/metadata only) use only allowlisted fields.

**Evidence:** openclaw@203a896:skills/weather/SKILL.md#L1-L5 ('name: weather / description: ... / homepage: https://wttr.in/:help / metadata:'). Evidence files skills-ref-openclaw-{weather,1password,peekaboo}.txt, identical key line in each: 'Unexpected fields in frontmatter: homepage. Only allowed-tools, compatibility, description, license, metadata, name are allowed.' + 'EXIT CODE: 1'. Catalog count via awk over all skills/*/SKILL.md first frontmatter block: total=52 with_homepage=33.

**Note:** Skills used are harness-shipped (repo-root skills/), not .claude/skills. The metadata.openclaw nesting is the spec-compliant vendor-extension pattern; homepage is the single non-portable field.

### SKV-NANOCLAW — confirmed

**Claim (safe to cite):** NanoClaw skills (.claude/skills/ is the only skills dir in the repo — no separate harness registry dir exists): the two richest-frontmatter skills FAIL — get-qodo-rules (5 fields: name, description, version, allowed-tools, triggers) and qodo-pr-resolver (4 fields: name, description, version, triggers) both exit 1 with 'Unexpected fields in frontmatter: triggers, version.' — while spec-allowlisted `allowed-tools` is accepted without complaint. A minimal name+description skill (setup) PASSES with exit 0 'Valid skill'.

**Evidence:** nanoclaw@aecad86:.claude/skills/get-qodo-rules/SKILL.md#L1-L6 ('name: get-qodo-rules / description: ... / version: 2.0.0 / allowed-tools: ["Bash"] / triggers:'). Evidence files: skills-ref-nanoclaw-get-qodo-rules.txt and skills-ref-nanoclaw-qodo-pr-resolver.txt both contain 'Unexpected fields in frontmatter: triggers, version. Only allowed-tools, compatibility, description, license, metadata, name are allowed.' + 'EXIT CODE: 1'; skills-ref-nanoclaw-setup.txt contains 'Valid skill: checkouts/nanoclaw/.claude/skills/setup' + 'EXIT CODE: 0'.

**Note:** NanoClaw has no harness-shipped/registry skills dir distinct from .claude/skills, so those were used (flagged as Claude-Code-format). Its vendor fields version/triggers are top-level and therefore non-portable; only its allowed-tools usage is spec-clean.

### SKV-ZEROCLAW — confirmed

**Claim (safe to cite):** ZeroClaw ships no harness-native skill files in the repo — native skills live in the user workspace (~/.zeroclaw/workspace/skills/ per docs), so the repo's .claude/skills/ (Claude Code dev-tooling skills, explicitly not harness-registry skills) were validated. Pattern: skills with plain name+description frontmatter (zeroclaw, changelog-generation) PASS exit 0; github-pr, authored as plain markdown with no YAML frontmatter at all, FAILS exit 1 with 'SKILL.md must start with YAML frontmatter (---)'. No SKILL.md anywhere in the repo uses author/version/category/tags top-level fields, although ZeroClaw's native frontmatter schema supports name, description, license, author, version, category, tags — of which only license overlaps the skills-ref allowlist, so ZeroClaw-native author/version/category/tags would hard-fail portable validation if emitted top-level.

**Evidence:** Evidence files: skills-ref-zeroclaw-zeroclaw.txt and skills-ref-zeroclaw-changelog-generation.txt ('Valid skill: ...' / 'EXIT CODE: 0'); skills-ref-zeroclaw-github-pr.txt ('- SKILL.md must start with YAML frontmatter (---)' / 'EXIT CODE: 1'). Native schema: zeroclaw@c6f49ca:crates/zeroclaw-runtime/src/skills/frontmatter.rs#L86-L112 (fields 'license', 'author', 'version', 'category', 'tags'); zeroclaw@c6f49ca:docs/book/src/tools/skills.md#L40-L53 ('Supported frontmatter fields are `name`, `description`, `version`, `author`, and `tags`.'). Absence claims: `find . -name SKILL.md -not -path './.context/*' -not -path './node_modules/*'` → only the 9 files under .claude/skills/; `grep -rlE "^(author|version|category|tags):" --include=SKILL.md .claude crates dev src` → no matches, exit 1.

**Note:** The requested author/version/category/tags ZeroClaw skill does not exist as a file in the repo — the fields exist only in the runtime parser and a docs example — so the richest available .claude/skills were used plus one zero-frontmatter skill to expose the missing-frontmatter failure mode. Three of the nine ZeroClaw .claude skills (github-pr, github-issue, squash-merge) have no frontmatter and would all fail identically.


## Amendment (2026-07-02, post-persona-review)

**SKV-NANOCLAW — superseded in part.** The original entry's note "NanoClaw has no harness-shipped/registry skills dir distinct from .claude/skills" was wrong: NC-SKILLS-1 already established the two-plane split (.claude/skills/ = operator plane, never loaded by NanoClaw code; container/skills/ = product plane, mounted into agent containers). A fresh validator run over all 8 container/skills/ skills passes 8/8 (evidence/skills-ref-nanoclaw-container-skills.txt — the superseding transcript). The original .claude/skills sample results stand as facts about the operator plane only (2 vendored Qodo skills fail on `triggers`/`version`). NanoClaw's U-SKILLS gap is therefore a CI job, not skill fixes.
